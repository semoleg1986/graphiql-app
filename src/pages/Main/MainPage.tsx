import i18next from 'i18next';
import GraphiQL from '../../components/Converter/Graphql';

const MainPage = () => {
  return (
    <div>
      <GraphiQL />
    </div>
  );
};

export default MainPage;
